---
phase: 03-workflows
plan: 02
type: execute
wave: 3
depends_on: ["02-01"]
files_modified: [
  "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt",
  "vault-desktop-tauri/src-tauri/src/network.rs"
]
autonomous: true
requirements: [UNLOCK-FLOW]

must_haves:
  truths:
    - "Mobile app receives unlock_request via WebSocket"
    - "Mobile app triggers Biometric prompt for approval"
    - "Desktop app mounts and unlocks after mobile approval"
  artifacts:
    - path: "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
      provides: "Unlock approval logic"
  key_links:
    - from: "vault-desktop-tauri/src-tauri/src/lib.rs"
      to: "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
      via: "Backend Push/Relay"
---

<objective>
Complete the 'Unlock' flow: Desktop Click -> Mobile Signal -> Biometric Approve -> Desktop Unlocks. This delivers the 'Magic Unlock' experience.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt
@vault-desktop-tauri/src-tauri/src/network.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle Unlock Signal on Mobile</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</files>
  <action>
    Update `MainActivity.kt` to handle `WAKE_UP_BIOMETRIC` or similar signal from the WebSocket.
    When received, show a system notification (if in background) or a dialog (if in foreground) that triggers `authenticateForUnlock`.
    Ensure the challenge is signed and sent back to the backend.
  </action>
  <verify>
    <automated>grep -q "WAKE_UP_BIOMETRIC" vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</automated>
  </verify>
  <done>Mobile app can be woken up remotely to authorize an unlock.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Desktop Mounting on Approval</name>
  <files>vault-desktop-tauri/src-tauri/src/network.rs</files>
  <action>
    Verify `handle_server_message` in `vault-desktop-tauri/src-tauri/src/network.rs` correctly handles `unlock_approved`.
    It should decrypt the `encrypted_key`, update `SharedKey`, and emit `vault-do-mount`.
    Already implemented, but ensure it works with the latest backend payload structure.
  </action>
  <verify>
    <automated>grep -q "vault-do-mount" vault-desktop-tauri/src-tauri/src/network.rs</automated>
  </verify>
  <done>Desktop app reacts correctly to mobile unlock approval.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Unlock Approval | mitigate | Mobile signs the challenge using hardware-backed Ed25519 key; desktop verifies identity. |
</threat_model>

<success_criteria>
Desktop unlocks seamlessly after biometric approval on the phone.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflows/03-02-SUMMARY.md`
</output>
