---
phase: 03-workflows
plan: 01
type: execute
wave: 3
depends_on: ["02-01"]
files_modified: [
  "vault-desktop-tauri/src-tauri/src/lib.rs",
  "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
]
autonomous: true
requirements: [SETUP-FLOW, DESKTOP-QR-PAYLOAD]

must_haves:
  truths:
    - "Desktop QR code contains backend_url and desktop public keys"
    - "Mobile app successfully scans QR and initiates setup"
    - "Desktop app shows 24 words after successful mobile pairing"
  artifacts:
    - path: "vault-desktop-tauri/src-tauri/src/lib.rs"
      provides: "Correct QR payload generation"
  key_links:
    - from: "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
      to: "vault-desktop-tauri/src-tauri/src/lib.rs"
      via: "QR Code Scanning"
---

<objective>
Complete the '1st Setup' flow: Desktop QR -> Mobile Scan -> Mobile becomes Key -> Desktop shows 24 words. This ensures the workstation and phone are cryptographically linked.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vault-desktop-tauri/src-tauri/src/lib.rs
@vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Desktop QR Payload</name>
  <files>vault-desktop-tauri/src-tauri/src/lib.rs</files>
  <action>
    Review `generate_desktop_identity` in `vault-desktop-tauri/src-tauri/src/lib.rs`.
    Ensure it includes `desktop_public_key`, `desktop_x_public_key`, `pairing_nonce`, and `backend_url`.
    Verified: it already seems to include these in the JSON return.
  </action>
  <verify>
    <automated>grep -q "backend_url" vault-desktop-tauri/src-tauri/src/lib.rs</automated>
  </verify>
  <done>Desktop QR payload is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Handle setup signal on Mobile</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</files>
  <action>
    Update `MainActivity.kt` to handle the `setup_vault` signal.
    When an onboarding QR is scanned, ensure the mobile app properly pairs and triggers the `share_master_key_with_phone` path on the desktop via the backend relay.
    Ensure the `encrypted_blob` containing the master key is received and decrypted.
  </action>
  <verify>
    <automated>grep -q "share_master_key_with_phone" vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</automated>
  </verify>
  <done>Mobile setup flow is fully integrated with desktop pairing.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | Master Key Transfer | mitigate | Master Key is encrypted for Mobile X25519 public key before being relayed through backend. |
</threat_model>

<success_criteria>
Workstation and Phone are linked, and the Master Recovery Key is displayed on the workstation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflows/03-01-SUMMARY.md`
</output>
