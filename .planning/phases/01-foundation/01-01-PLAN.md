---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "vault-backend-go/cmd/ws_test/main.go",
  "vault-backend-go/internal/api/handlers.go",
  "vault-backend-go/internal/websocket/hub.go"
]
autonomous: true
requirements: [BACKEND-01, BACKEND-02]

must_haves:
  truths:
    - "Backend handles mobile_register message over WebSocket"
    - "Backend can route push signals to specific mobile public keys"
    - "Backend compiles without unused import errors"
  artifacts:
    - path: "vault-backend-go/internal/websocket/hub.go"
      provides: "Improved identity binding"
    - path: "vault-backend-go/internal/api/handlers.go"
      provides: "Handshake for mobile devices"
  key_links:
    - from: "vault-backend-go/internal/api/handlers.go"
      to: "vault-backend-go/internal/websocket/hub.go"
      via: "hub.BindIdentity call"
---

<objective>
Fix backend 'Variable not used' errors and enhance WebSocket signaling to support mobile device registration. This ensures the 'Relay' mechanism can target mobile devices directly.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vault-backend-go/internal/api/handlers.go
@vault-backend-go/internal/websocket/hub.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix unused imports in backend cmd tools</name>
  <files>vault-backend-go/cmd/ws_test/main.go</files>
  <action>Remove unused "net/http" import in `vault-backend-go/cmd/ws_test/main.go`. Verify the file compiles with `go build ./...` in the backend directory.</action>
  <verify>
    <automated>cd vault-backend-go && go build ./cmd/ws_test/main.go</automated>
  </verify>
  <done>Backend test tools compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement mobile_register in WebSocket handlers</name>
  <files>vault-backend-go/internal/api/handlers.go</files>
  <action>
    Update `readPump` in `vault-backend-go/internal/api/handlers.go` to handle a new message type: `mobile_register`.
    This should call `h.hub.BindIdentity` using the provided `public_key`.
    The `pairing_nonce` field should be optional for mobile (can be empty string).
  </action>
  <verify>
    <automated>grep -q "mobile_register" vault-backend-go/internal/api/handlers.go</automated>
  </verify>
  <done>Backend supports mobile registration via WebSocket.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Spoofing | WS mobile_register | mitigate | Mobile public key is used as identity; subsequent push relays target this key. Verification of signature in RelayPush is recommended in future. |
</threat_model>

<success_criteria>
Backend compiles and supports explicit mobile device identification on WebSocket connections.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
