---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "vault-mobile-auth/app/build.gradle.kts",
  "vault-mobile-auth/app/src/main/java/com/vault/auth/SecureStorageManager.kt",
  "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
]
autonomous: true
requirements: [MOBILE-SECURE-STORAGE]

must_haves:
  truths:
    - "Sensitive keys (AES key, PIN hash) are stored in EncryptedSharedPreferences"
    - "Master key material is never stored in plaintext"
  artifacts:
    - path: "vault-mobile-auth/app/src/main/java/com/vault/auth/SecureStorageManager.kt"
      provides: "Secure key/value storage abstraction"
  key_links:
    - from: "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
      to: "vault-mobile-auth/app/src/main/java/com/vault/auth/SecureStorageManager.kt"
      via: "Dependency injection/instantiation"
---

<objective>
Implement SecureStorageManager using Android Jetpack Security (EncryptedSharedPreferences) to protect the Master Key and security settings on the mobile device.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vault-mobile-auth/app/build.gradle.kts
@vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Security Crypto dependency</name>
  <files>vault-mobile-auth/app/build.gradle.kts</files>
  <action>Add `implementation("androidx.security:security-crypto:1.1.0-alpha06")` to the dependencies block in `vault-mobile-auth/app/build.gradle.kts`.</action>
  <verify>
    <automated>grep -q "security-crypto" vault-mobile-auth/app/build.gradle.kts</automated>
  </verify>
  <done>Dependency added for EncryptedSharedPreferences.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SecureStorageManager class</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/SecureStorageManager.kt</files>
  <action>
    Create `SecureStorageManager.kt`. 
    Implement a wrapper around `EncryptedSharedPreferences` for common operations:
    - `saveString(key: String, value: String)`
    - `getString(key: String): String?`
    - `saveBoolean(key: String, value: Boolean)`
    - `getBoolean(key: String, default: Boolean): Boolean`
    - `clear()`
    Use `MasterKey.Builder(context).setKeyScheme(MasterKey.KeyScheme.AES256_GCM).build()` for the master key.
  </action>
  <verify>
    <automated>ls vault-mobile-auth/app/src/main/java/com/vault/auth/SecureStorageManager.kt</automated>
  </verify>
  <done>SecureStorageManager is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate MainActivity to SecureStorageManager</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</files>
  <action>
    Update `MainActivity.kt` to use `SecureStorageManager` instead of raw `getSharedPreferences("vault_auth", MODE_PRIVATE)`.
    Specifically update `saveSetup`, `isSecuritySetupComplete`, `verifyPin`, `enterDecoyMode`, `sendUnlockApproval`, `pairWithDesktop`, and the `LaunchedEffect` in `onCreate`.
  </action>
  <verify>
    <automated>grep -q "SecureStorageManager" vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</automated>
  </verify>
  <done>MainActivity uses secure storage for all sensitive data.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | SharedPreferences | mitigate | Use EncryptedSharedPreferences to encrypt data at rest using Android Keystore. |
</threat_model>

<success_criteria>
Sensitive mobile data is encrypted at rest using hardware-backed keys.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
