---
phase: 02-signaling
plan: 01
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "vault-mobile-auth/app/src/main/java/com/vault/auth/WebSocketService.kt",
  "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
]
autonomous: true
requirements: [MOBILE-WS-SIGNALING]

must_haves:
  truths:
    - "Mobile app establishes WebSocket connection to backend on launch"
    - "Mobile app registers its public key with the backend via mobile_register"
    - "Mobile app receives push_relay messages from the backend"
  artifacts:
    - path: "vault-mobile-auth/app/src/main/java/com/vault/auth/WebSocketService.kt"
      provides: "Persistent background signaling connection"
  key_links:
    - from: "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt"
      to: "vault-mobile-auth/app/src/main/java/com/vault/auth/WebSocketService.kt"
      via: "Service binding or direct instantiation"
---

<objective>
Implement WebSocketService in Kotlin to maintain a background connection to the Go backend, allowing the mobile app to receive signals (like unlock requests) in real-time.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt
@vault-backend-go/internal/api/handlers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WebSocketService</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/WebSocketService.kt</files>
  <action>
    Create `WebSocketService.kt` using OkHttp's `WebSocketListener`.
    The service should:
    - Connect to the backend WebSocket URL (derive from `Constants.BASE_URL`).
    - On connection, send a `mobile_register` message with the mobile's public key.
    - Implement a listener callback to notify `MainActivity` of incoming messages.
    - Handle `push_relay` messages by parsing the `encrypted_blob`.
    - Implement simple reconnection logic on failure.
  </action>
  <verify>
    <automated>ls vault-mobile-auth/app/src/main/java/com/vault/auth/WebSocketService.kt</automated>
  </verify>
  <done>WebSocketService is implemented and capable of receiving relay messages.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate WebSocketService into MainActivity</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</files>
  <action>
    Instantiate and start `WebSocketService` in `MainActivity.onCreate`.
    Pass a callback to handle incoming signals.
    For now, log incoming `push_relay` messages.
  </action>
  <verify>
    <automated>grep -q "WebSocketService" vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</automated>
  </verify>
  <done>MainActivity connects to the signaling server on startup.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Denial of Service | WebSocket Connection | mitigate | Implement exponential backoff for reconnections to avoid hammering the backend. |
</threat_model>

<success_criteria>
Mobile app maintains a live signaling connection and identifies itself to the backend.
</success_criteria>

<output>
After completion, create `.planning/phases/02-signaling/02-01-SUMMARY.md`
</output>
