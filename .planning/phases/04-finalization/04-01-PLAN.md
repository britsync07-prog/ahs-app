---
phase: 04-finalization
plan: 01
type: execute
wave: 4
depends_on: ["03-01", "03-02"]
files_modified: [
  "vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt",
  "vault-desktop-tauri/src/App.tsx"
]
autonomous: true
requirements: [MOBILE-WARNINGS, DESKTOP-WARNINGS, QA-PASS]

must_haves:
  truths:
    - "Mobile app compiles without unused variable warnings"
    - "Desktop app compiles without unused variable warnings"
    - "E2E Phone as Key flow works perfectly"
  artifacts: []
  key_links: []
---

<objective>
Clean up 'Variable not used' warnings in Mobile and Desktop codebases and perform a final QA pass on the 'Phone as Key' workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt
@vault-desktop-tauri/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up unused variables in Mobile</name>
  <files>vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</files>
  <action>
    Remove unused variables in `MainActivity.kt`:
    - `pairingNonce`
    - `showPinPrompt`
    - `unlockPin`
    - Any others identified by the Kotlin compiler.
  </action>
  <verify>
    <automated>grep -v "pairingNonce" vault-mobile-auth/app/src/main/java/com/vault/auth/MainActivity.kt</automated>
  </verify>
  <done>Mobile codebase is clean of unused variable warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up unused variables in Desktop</name>
  <files>vault-desktop-tauri/src/App.tsx</files>
  <action>
    Review `vault-desktop-tauri/src/App.tsx` and related components for unused imports or variables.
    Remove them to ensure a clean build.
  </action>
  <verify>
    <automated>grep -v "unused-var" vault-desktop-tauri/src/App.tsx</automated>
  </verify>
  <done>Desktop codebase is clean of warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full 'Phone as Key' workflow including setup and unlock.</what-built>
  <how-to-verify>
    1. Run Backend.
    2. Run Desktop app.
    3. Run Mobile app.
    4. Perform '1st Setup' flow: scan QR, complete setup on mobile, verify 24 words on desktop.
    5. Lock vault.
    6. Perform 'Unlock' flow: click unlock on desktop, approve on mobile, verify desktop unlocks and mounts.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | General | Code Quality | mitigate | Clean up warnings to reduce technical debt and potential bugs. |
</threat_model>

<success_criteria>
Project is warning-free and the 'Phone as Key' feature is fully functional and secure.
</success_criteria>

<output>
After completion, create `.planning/phases/04-finalization/04-01-SUMMARY.md`
</output>
